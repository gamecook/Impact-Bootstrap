ig.module(
    'game.bootstrap.weltmeister-bootstrap'
)
    .requires(
    'game.bootstrap.plugins.weltmeister.entities',
    'game.bootstrap.plugins.weltmeister.random-map'

)
    .defines(function ()
    {

        /* Empty module to require all bootstrap welmeister plugins */

    });